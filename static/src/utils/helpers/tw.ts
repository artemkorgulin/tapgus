/**
 * This function is used for prettier-plugin-tailwindcss and prettier-plugin-classnames
 * to work with simple variables. See "tailwindFunctions" and "customFunctions"
 * in .prettierrc.json and experimental.classRegex in the tailwind config in IDE
 */
export const tw = (classNames: string): string => classNames;
