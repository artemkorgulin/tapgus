export enum BRIDGE_TYPE {
    APP = 'react-app-bridge',
}

export enum BRIDGE_ACTION {
    NOTIFY = 'notify',
    NOTIFY_UPDATE = 'notify-upd-status-by-ids',
    NOTIFY_REMOVE = 'notify-remove-by-id',
    NAVIGATE = 'navigate',
    CLICK = 'click',
}
