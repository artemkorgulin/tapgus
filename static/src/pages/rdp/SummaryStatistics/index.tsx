import type { TComponent } from 'utils/types/elements';

import { SummaryStatisticsTable } from 'components/domains/SummaryStatistics/SummaryStatisticTable/index';

const SummaryStatisticsPage: TComponent = () => {
    return (
        <SummaryStatisticsTable
        // rawData={null}
        // errorMessage={'error'}
        // isLoading={false}
        />
    );
};

export default SummaryStatisticsPage;
